%h1 Listing groups

%table.table.table-sm.table-hover.table-striped
  %thead
    %tr
      %th Name
      %th Status
      %th Contacts Count
      %th Actions

  %tbody
    - @groups.each do |group|
      %tr
        %td= group.name
        %td= group.active ? 'Active' : 'Inactive'
        %td= group.contacts.count
        %td
          = link_to 'New Contact', new_group_contact_path(group)
          |
          = link_to 'Contacts', group_contacts_path(group)
          |
          = link_to 'Show', group
          |
          = link_to 'Edit', edit_group_path(group)
          |
          = link_to 'Destroy', group, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Group', new_group_path
